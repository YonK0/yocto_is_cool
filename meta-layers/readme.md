# Meta Layers Directory :
- **openembedded-core** (styhead branch)
- **bitbake** (2.10 release) 
- **meta-raspberrypi** (styhead branch)


These layers are automatically cloned when we building our kas ......